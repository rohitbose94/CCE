function CCE_Stimulus(stim_type,distractor_type)

% if ~exist('electrode_num','var') || isempty(server)
%     
% end
if(stim_type=='Pneumatic')
    disp("Yup... Running");
    Trials=10;
    

% xippmex('digiout',1,1);
% pause(0.1);
% xippmex('digiout',1,0);